% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/methods-PACEFit.R
\name{cellContamination}
\alias{cellContamination}
\alias{cellContamination,PACEFit-method}
\title{Per-cell contamination}
\usage{
cellContamination(object, ...)

\S4method{cellContamination}{PACEFit}(object, ...)
}
\arguments{
\item{object}{A \link{PACEFit} fitted with \code{contamination = "percell_hc"}.}

\item{...}{Unused.}
}
\value{
A data frame with one row per cell, in fitting order: \code{cell} (the
working-frame cell identifier, or the row index if the frame is unnamed),
\code{celltype} (the label the cell was fitted under), \code{rho} (the contamination
loading) and \code{contamFraction}.
}
\description{
Returns the per-cell contamination loading \code{rho_i} and the fraction of a
cell's expected counts attributed to its local ambient field. Under the
per-cell contamination model the expected count is
\code{mu_ig = mu_bio_ig + rho_i * a_ig}, where \code{a_ig} is the cross-cell-type
ambient field at cell \code{i} and \code{rho_i} is a single empirical-Bayes shrunken
loading shared across genes. The contamination fraction summarises that
second term over genes,
\verb{contamFraction_i = sum_g mu_spill_ig / sum_g mu_ig}, and is the quantity
reported as \code{contam_frac} in the solver's fitting trace.
}
\details{
A high contamination fraction marks a cell whose profile is substantially
explained by its neighbours rather than by its own cell type, which is the
expected signature of a segmentation or transcript-assignment error. It is
not a complete cell-quality score: because \code{rho_i} carries a fixed
gene-direction (the local ambient), it will under-report contamination whose
direction departs from the neighbourhood average, and a cell that is wholly a
segmentation artefact may fit some incorrect cell type with a low \code{rho_i}.
Pair it with ordinary per-cell quality control.

Read \code{contamFraction} rather than \code{rho} on its own. The ambient field is
cross-cell-type, so a cell with no differently-typed neighbour inside the
technical kernel has \code{a_ig = 0} for every gene and therefore no contamination
at all. Its \code{rho_i} is then unidentified and shrinks to the empirical Bayes
prior mean, giving every such cell the same apparently middling loading. On
the shipped example fit this is 4,589 of 7,898 cells, 84\% of the tumour cells
in a tumour-dominated crop. \code{contamFraction} reports them as zero, which is
correct; \code{rho} alone does not.
}
\examples{
fit <- readRDS(system.file("extdata", "pace_fit_example.rds", package = "PACE"))
cc <- cellContamination(fit)
head(cc)
# Contamination is highest where cells sit against a different type.
tapply(cc$contamFraction, cc$celltype, median)
}
