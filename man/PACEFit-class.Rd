% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R, R/methods-PACEFit.R
\docType{class}
\name{PACEFit}
\alias{PACEFit}
\alias{show,PACEFit-method}
\title{PACEFit: a fitted PACE model}
\usage{
\S4method{show}{PACEFit}(object)
}
\description{
An S4 container for the result of a PACE fit. It holds the raw penalised
quasi-likelihood solver state together with the three interpretable output
layers: shrunken per-(gene, focal, neighbour) proximity slopes, the per-gene
variance decomposition, and the per-pair driver tables. Construct one with
\code{\link[=paceFit]{paceFit()}}; read it out with \code{\link[=neighbourSlopes]{neighbourSlopes()}}, \code{\link[=varianceDecomposition]{varianceDecomposition()}},
and \code{\link[=topDrivers]{topDrivers()}}.
}
\section{Slots}{

\describe{
\item{\code{fit}}{The raw streaming PQL solver state (fixed and random effects,
per-cell contamination loadings, gene-wise overdispersions, variance
components).}

\item{\code{neighbourSlopes}}{A data frame of shrunken proximity coefficients, one row
per (gene, focal cell type, neighbour cell type\link{, condition term}), with the
raw estimate, shrunken estimate, and local false sign rate (lfsr).}

\item{\code{varianceDecomposition}}{A list of per-gene, per-focal variance
decomposition tables (cell-type identity, spatial cell state, contamination,
residual; and, for condition cohorts, a responder spatial block).}

\item{\code{topDrivers}}{A list of per-pair driver-score tables ranking the genes that
mediate each focal-neighbour spatial relationship.}

\item{\code{cellTypes}}{The cell types modelled, in fitting order.}

\item{\code{params}}{The fitting parameters actually used (bandwidths, contamination
and dispersion settings, condition column, and so on).}

\item{\code{context}}{The working frame and fixed-effect design retained from the fit
so that the downstream stages (\code{\link[=paceShrink]{paceShrink()}}, \code{\link[=paceDecompose]{paceDecompose()}},
\code{\link[=paceDrivers]{paceDrivers()}}) can run without refitting.}
}}

\examples{
fit <- readRDS(system.file("extdata", "pace_fit_example.rds", package = "PACE"))
fit
neighbourSlopes(fit)
}
