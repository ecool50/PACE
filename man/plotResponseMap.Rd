% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotResponseMap}
\alias{plotResponseMap}
\title{Tissue maps and scatters for one exemplar image per arm}
\usage{
plotResponseMap(
  object,
  spe,
  gene,
  focal,
  neighbour,
  images = NULL,
  kde_h = 60,
  ngrid = 200,
  scalebar_len = 100
)
}
\arguments{
\item{object}{A \link{PACEFit} fitted with a \code{condition_col}.}

\item{spe}{The \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} that was fitted.}

\item{gene, focal, neighbour}{Gene, focal cell type and neighbour cell type.}

\item{images}{Optional named character vector of image identifiers, one per
arm. The default picks, for each arm, the image with the most focal cells.}

\item{kde_h, ngrid}{Kernel bandwidth and grid size for the density map.}

\item{scalebar_len}{Scale-bar length in micrometres.}
}
\value{
A \code{patchwork} object.
}
\description{
The spatial view behind \code{\link[=plotResponseCurve]{plotResponseCurve()}}. For one image per condition
arm: a smooth kernel-density map of the neighbour type with the focal cells
overlaid and coloured by expression (top), and expression against neighbour
density for the same cells with a fitted line (bottom). Colour scales are
shared across the panels so the arms are directly comparable.
}
\examples{
spe <- readRDS(system.file("extdata", "mel_cosmx_subset.rds", package = "PACE"))
\donttest{
fit <- paceFit(spe, celltype_col = "cellType", condition_col = "Responder",
               image_col = "image", kernel_per_image = TRUE,
               image_re = "intercept", verbose = FALSE)
plotResponseMap(fit, spe, "SPP1", "Macrophage", "Tumour")
}
}
