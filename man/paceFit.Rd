% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/paceFit.R
\name{paceFit}
\alias{paceFit}
\alias{paceFit,SpatialExperiment-method}
\title{Fit a PACE model to a SpatialExperiment}
\usage{
paceFit(object, ...)

\S4method{paceFit}{SpatialExperiment}(
  object,
  celltype_col,
  image_col = NULL,
  condition_col = NULL,
  assay_name = "counts",
  h_bio = 30,
  h_tech = 5,
  contamination = c("percell_hc", "none"),
  dispersion = c("nb1", "nb2"),
  kernel_per_image = FALSE,
  image_re = c("none", "intercept", "slopes", "condition_slopes"),
  resp_term = NULL,
  pairs = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} with a counts assay and
two-dimensional spatial coordinates.}

\item{...}{Further arguments passed to the streaming fitter (for example
\code{n_iter}, \code{threads}, \code{chunk_size}).}

\item{celltype_col}{Name of the \link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::colData} column holding
the discrete cell-type annotation.}

\item{image_col}{Optional name of the colData column grouping cells into images
or samples. If \code{NULL} (default) the whole object is treated as one section.}

\item{condition_col}{Optional name of a colData column holding a binary
condition (for example treatment response); enables the responder spatial
block for multi-sample cohorts.}

\item{assay_name}{Name of the counts assay to use (default \code{"counts"}).}

\item{h_bio, h_tech}{Biological and technical kernel bandwidths in micrometres
(defaults 30 and 5).}

\item{contamination}{Contamination correction: \code{"percell_hc"} (default,
per-cell homotypic-core ambient term) or \code{"none"}.}

\item{dispersion}{Negative binomial parameterisation, \code{"nb1"} (default) or
\code{"nb2"}.}

\item{kernel_per_image}{If \code{TRUE}, neighbourhoods are built within each image
(use when images are separate samples/patients). Default \code{FALSE}.}

\item{image_re}{Second image-level random-effect block: one of \code{"none"},
\code{"intercept"}, \code{"slopes"}, \code{"condition_slopes"}.}

\item{resp_term}{For condition cohorts, the responder interaction term prefix
used when shrinking and decomposing (for example \code{"ResponderPD"}). If \code{NULL}
it is derived from \code{condition_col}.}

\item{pairs}{Optional list of focal-neighbour cell-type pairs for the driver
tables; if \code{NULL}, all pairs are scored.}

\item{verbose}{Whether to print progress.}
}
\value{
A \link{PACEFit} object.
}
\description{
\code{paceFit()} runs the full PACE pipeline on a
\link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment}: it builds the biological and technical
neighbourhood kernels from the spatial coordinates and cell-type labels, fits
the hierarchical negative binomial mixed model by streaming penalised
quasi-likelihood (with the per-cell contamination correction), shrinks the
neighbour slopes with multivariate adaptive shrinkage, and computes the
variance decomposition and per-pair driver tables.
}
\details{
Cell-type labels, spatial coordinates, and (optionally) an image/sample grouping
and a condition are read from the object following SpatialExperiment
conventions. The defaults reproduce the manuscript recipe (30 um biological and
5 um technical bandwidths, per-cell homotypic-core contamination correction).
}
\examples{
\dontrun{
library(SpatialExperiment)
fit <- paceFit(spe, celltype_col = "cellType")
head(neighbourSlopes(fit))
}

}
