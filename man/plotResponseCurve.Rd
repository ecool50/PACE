% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotResponseCurve}
\alias{plotResponseCurve}
\title{Response curve: binned means with the PACE slope overlaid}
\usage{
plotResponseCurve(
  object,
  spe,
  gene,
  focal,
  neighbour,
  n_bins = 10,
  min_bin_n = 10,
  colours = c("#3B6FB6", "#C0392B")
)
}
\arguments{
\item{object}{A \link{PACEFit} fitted with a \code{condition_col}.}

\item{spe}{The \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} that was fitted.}

\item{gene, focal, neighbour}{Gene, focal cell type and neighbour cell type.}

\item{n_bins}{Number of density quantile bins (default 10).}

\item{min_bin_n}{Bins with fewer focal cells than this are dropped.}

\item{colours}{Colours for the two arms.}
}
\value{
A \code{ggplot} object.
}
\description{
Population view of one gene in \code{focal} cells against \code{neighbour} density,
split by the fit's condition. The solid line and ribbon are binned means and
standard errors of the observed data; the dashed line is the PACE slope read
straight from the fitted model, anchored at each arm's data centroid. It is
the model's estimate drawn over the data, not a smoother refitted to it.
}
\examples{
spe <- readRDS(system.file("extdata", "mel_cosmx_subset.rds", package = "PACE"))
\donttest{
fit <- paceFit(spe, celltype_col = "cellType", condition_col = "Responder",
               image_col = "image", kernel_per_image = TRUE,
               image_re = "intercept", verbose = FALSE)
plotResponseCurve(fit, spe, "SPP1", "Macrophage", "Tumour")
}
}
